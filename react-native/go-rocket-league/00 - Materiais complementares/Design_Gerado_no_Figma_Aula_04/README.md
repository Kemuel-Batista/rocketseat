
  # High-fidelity mobile game UI

  This is a code bundle for High-fidelity mobile game UI. The original project is available at https://www.figma.com/design/UU19tgINmdaeBoquuIs6eB/High-fidelity-mobile-game-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  